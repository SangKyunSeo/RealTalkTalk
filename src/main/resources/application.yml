spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3310/realtalktalk?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: qaz0802

    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          format_sql: true
          show_sql: true

    logging:
      level:
        org.hibernate:
          type.descriptor.sql: trace
          SQL: DEBUG

jwt:
  secret: VlwEyVBsYt9V7zq57TejMnVUyzblYcfPQye08f7MGVA9XkHa